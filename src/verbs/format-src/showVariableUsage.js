import _ from 'lodash';
import randomcolor from 'randomcolor';

import {
  wrap,
  substring,
  columnize,
  inHtml
} from './utils';

export default (src, scopes) => {
  const lines = src.split('\n');
};
